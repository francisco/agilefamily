<h1>Weekly tasks</h1>
<div ng-app="taskSchedule">
  <div ng-controller="NamesController">
    <div class="btn-group">
      <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown">
        {{username}} <span class="caret"></span>
      </button>
      <ul class="dropdown-menu" role="menu">
        <li ng-repeat="(key,val) in appData.familyMembers" ng-click="fetch(val['id'],val['name'])">{{val['name']}}</li>
      </ul>
      <!-- <select ng-change="fetch()" ng-model="selectedName" ng-options="val as val['name'] for (key,val) in appData.familyMembers"></select> -->
    </div><br /><br />
    <table class="table table-bordered">
      <tr>
        <th>Monday</th>
        <th>Tuesday</th>
        <th>Wednesday</th>
        <th>Thursday</th>
        <th>Friday</th>
        <th>Saturday</th>
        <th>Sunday</th>
      </tr>
      <tr>
        <td id="taskData" ng-repeat="(day,task_descriptions) in taskData">
          <div ng-repeat="task in task_descriptions" ng-click="completeTask(task.id, task.taskWeekdayId)" ng-class="{active: task.complete == true}"> {{task.description}}<hr></div>
        </td>
      </tr>
    </table>
    <div ng-repeat="task in fbTasks">
      <table>
        <tr>
        <th>Monday</th>
        <th>Tuesday</th>
        <th>Wednesday</th>
        <th>Thursday</th>
        <th>Friday</th>
        <th>Saturday</th>
        <th>Sunday</th>
      </tr>
      <span ng-if="task.task.1 == true"></span>
    </div>
    <div class="panel panel-success">
      <div class="panel-heading"><strong>Add a Task</strong></div>
      <div class="panel-body">
        <form name="addTasker" role="form">
          <div class="btn-group">
            <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown">
              {{taskUsername}} <span class="caret"></span>
            </button>
            <ul class="dropdown-menu" role="menu">
              <li ng-repeat="(key,val) in appData.familyMembers" ng-click="getUser(val['id'],val['name'])">{{val['name']}}</li>
            </ul>
      <!-- <select ng-change="fetch()" ng-model="selectedName" ng-options="val as val['name'] for (key,val) in appData.familyMembers"></select> -->
          </div><br />
          <h4>Select Weekdays:</h4>
          <div class="btn-group">
            <label class="btn btn-default">
              <input type="checkbox" ng-model="newTask.1" ng-true-value="true" ng-false-value="false"> Monday
            </label>
            <label class="btn btn-default">
              <input type="checkbox" ng-model="newTask.2" ng-true-value="true" ng-false-value="false"> Tuesday
            </label>
            <label class="btn btn-default">
              <input type="checkbox" ng-model="newTask.3" ng-true-value="true" ng-false-value="false"> Wednesday
            </label>
            <label class="btn btn-default">
              <input type="checkbox" ng-model="newTask.4" ng-true-value="true" ng-false-value="false"> Thursday
            </label>
            <label class="btn btn-default">
              <input type="checkbox" ng-model="newTask.5" ng-true-value="true" ng-false-value="false"> Friday
            </label>
            <label class="btn btn-default">
              <input type="checkbox" ng-model="newTask.6" ng-true-value="true" ng-false-value="false"> Saturday
            </label>
            <label class="btn btn-default">
              <input type="checkbox" ng-model="newTask.7" ng-true-value="true" ng-false-value="false"> Sunday
            </label>
          </div><br />
          <h4>Input Task Description:</h4>
          <input type='text' ng-model="newTask.description" class="form-control"><br />
          <button ng-click="addNewTask()" class="btn btn-success">Submit</button>
        </form>
      </div>
    </div>
  </div>
</div>